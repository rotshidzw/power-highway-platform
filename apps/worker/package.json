{
  "name": "@nph/worker",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "lint": "eslint src --max-warnings=0",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@nph/contracts": "workspace:*",
    "@nph/db": "workspace:*",
    "@nph/ledger": "workspace:*",
    "bullmq": "^5.12.0"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "ts-node": "^10.9.2",
    "vitest": "^2.0.5"
  }
}
